---
title: 从零开始的代码练习
date: 2026-07-27 18:40:00
categories:
  - 编程
tags:
  - Python
  - 练习
description: 通过一个小函数，记录从需求到测试的最小实践闭环。
---

练习新知识时，先完成一个足够小、可以运行和验证的例子，通常比直接阅读大量资料更有效。

## 练习目标

写一个函数，判断一个字符串是否是回文。这里先忽略大小写和非字母数字字符。

```python
def is_palindrome(text: str) -> bool:
    normalized = "".join(char.lower() for char in text if char.isalnum())
    return normalized == normalized[::-1]


assert is_palindrome("level")
assert is_palindrome("A man, a plan, a canal: Panama")
assert not is_palindrome("learning")
```

## 复盘问题

- 输入为空字符串时，应返回什么？
- `isalnum()` 是否符合当前需求？
- 怎样为这个函数补充更多边界条件测试？

这些问题可以成为下一次练习的起点。
